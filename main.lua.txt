local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()

local Window = Rayfield:CreateWindow({
    Name = "MadoHub Language Selection",
    LoadingTitle = "MadoHub",
    LoadingSubtitle = "Choose Your Language",
    Theme = "Dark",
    DisableRayfieldPrompts = false,
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "MadoHub",
        FileName = "LanguageSettings"
    },
    Discord = { Enabled = false },
    KeySystem = false
})

local Tab = Window:CreateTab("Language Selection", 4483362458)

Tab:CreateSection("Select your preferred language:")

-- English Button
Tab:CreateButton({
    Name = "English",
    Callback = function()
        Rayfield:CreateNotification({
            Title = "Language Selected",
            Content = "English has been selected.",
            Duration = 3
        })
        -- You can add your English setup code here
    end
})

-- Arabic Button
Tab:CreateButton({
    Name = "العربية",
    Callback = function()
        Rayfield:CreateNotification({
            Title = "تنبيه",
            Content = "الترجمة العربية قد لا تكون دقيقة تمامًا. يرجى الملاحظة.",
            Duration = 5
        })
        -- You can add your Arabic setup code here
    end
})

-- Additional note paragraph
Tab:CreateParagraph({
    Title = "ملاحظة:",
    Content = "الترجمة العربية قد لا تكون دقيقة تمامًا. يرجى الملاحظة."
})

-- Additional note paragraph
Tab:CreateParagraph({
    Title = "Note:",
    Content = "Arabic Translation might not be the best"
})

